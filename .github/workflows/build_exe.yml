name: Build Windows EXE

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r Freedom-Force-The-Reckoning-game/requirements.txt
          pip install pyinstaller

      - name: Build EXE
        shell: cmd
        run: |
          pyinstaller --onefile --windowed ^
            --add-data "Freedom-Force-The-Reckoning-game/assets;assets" ^
            --add-data "Freedom-Force-The-Reckoning-game/current_enemy;current_enemy" ^
            --add-data "Freedom-Force-The-Reckoning-game/current_ok_boys;current_ok_boys" ^
            --add-data "Freedom-Force-The-Reckoning-game/final_enemy_iran;final_enemy_iran" ^
            --add-data "Freedom-Force-The-Reckoning-game/machine_learning;machine_learning" ^
            --add-data "Freedom-Force-The-Reckoning-game/data;data" ^
            Freedom-Force-The-Reckoning-game/main.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: Freedom-Force-The-Reckoning
          path: dist/main.exe
